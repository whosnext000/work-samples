select{
	padding: 5px;
	padding-right: 30px;
	width: auto;
	background-image:url(images/selectArrow.png);
	background-repeat: no-repeat;
	background-position: 90% center;
	background-color: transparent;
	border: 1px solid #{{borderColor}};
	-moz-box-shadow: 0 0 1px 0 #{{shadowColor}};
	-webkit-box-shadow: 0 0 1px 0 #{{shadowColor}};
	box-shadow: 0 0 1px 0 #{{shadowColor}};
	cursor: pointer;
	/* Chrome 10+,Safari */
	-webkit-transition: all {{transitionTime}}ms ease-in-out;
	/* Firefox 4+ */
	-moz-transition: all {{transitionTime}}ms ease-in-out;
	/* IE 9,10 */
	-ms-transition: all {{transitionTime}}ms ease-in-out;
	/* Opera */
	-o-transition: all {{transitionTime}}ms ease-in-out;
	/* W3C */
	transition: all {{transitionTime}}ms ease-in-out;
	-moz-border-radius: 0 0 1px 0 #{{borderRadiusColor}};
	-webkit-border-radius: 0 0 1px 0 #{{borderRadiusColor}};
	border-radius: 0 0 1px 0 #{{borderRadiusColor}};
	-webkit-appearance:none;
}

select:focus{
	-moz-box-shadow: 0 0 4px 0px #{{focusColor}};
	-webkit-box-shadow: 0 0 4px 0px #{{focusColor}};
	box-shadow: 0 0 4px 0px #{{focusColor}};
	outline: none;
}

select option{
	background-color:#{{colorOne}};
	-moz-box-shadow: 0 0 4px 0px #{{focusColor}};
	-webkit-box-shadow: 0 0 4px 0px #{{focusColor}};
	box-shadow: 0 0 4px 0px #{{focusColor}};
	border: 1px solid #{{optionBorderColor}};
}

#select-style{
	background: #{{colorOne}}; /* Old browsers */
	background: -moz-linear-gradient(top,  #{{colorOne}} 0%, #{{colorTwo}} 100%); /* FF3.6+ */
	background: -webkit-gradient(linear, left top, left bottom, color-stop(0%,#{{colorOne}}), color-stop(100%,#{{colorTwo}})); /* Chrome,Safari4+ */
	background: -webkit-linear-gradient(top,  #{{colorOne}} 0%,#{{colorTwo}} 100%); /* Chrome10+,Safari5.1+ */
	background: -o-linear-gradient(top,  #{{colorOne}} 0%,#{{colorTwo}} 100%); /* Opera 11.10+ */
	background: -ms-linear-gradient(top,  #{{colorOne}} 0%,#{{colorTwo}} 100%); /* IE10+ */
	background: linear-gradient(to bottom,  #{{colorOne}} 0%,#{{colorTwo}} 100%); /* W3C */
	filter: progid:DXImageTransform.Microsoft.gradient( startColorstr='#{{colorOne}}', endColorstr='#{{colorTwo}}',GradientType=0 ); /* IE6-9 */
	overflow: hidden;
	display: table;
}
